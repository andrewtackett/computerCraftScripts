-- Command to get starting coordinates for inputting into config for storage location

---@diagnostic disable-next-line: undefined-global
print(gps.locate())

local version = 1
return {
    version = version
}
